# INGEC Backend Server

## Seeding the Database
To populate your Supabase database with realistic sample projects and contractors for demonstration:
1. Ensure your `.env` file contains the `SUPABASE_SERVICE_KEY`.
2. Run `npm run seed`.
